<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= t ".title" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

  <div class="container mt-4">
    <!-- Header Section -->
    <h1 class="text-center mb-4"><%= t ".title" %></h1>

    <!-- Authentication Links -->
    <div class="d-flex justify-content-end mb-4">
      <%= link_to "New product", new_product_path, class: 'btn btn-primary me-2' if authenticated? %>
      <%= link_to "Login", new_session_path, class: 'btn btn-secondary' unless authenticated? %>
    </div>

    <!-- Products List -->
    <div id="products">
      <% @products.each do |product| %>
        <div class="card mb-3">
          <div class="card-body">
            <%= link_to product.name, product, class: 'card-title h5' %>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
